/* Object literal pattern */
var modal = {
  init: function() {

  }
};

/* Revealing module pattern / written like a Class */
function Popup(config) {
  var settings = $.extend({
    class: ""
  }, config);

  function init() {

  }

  init();

  return {

  };
}
